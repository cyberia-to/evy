[package]
name = "evy_platform_caps"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "evy: runtime platform capability probe (engines, backends, thermal class)"

[dependencies]
unimem = { workspace = true, optional = true }

[features]
default = []
# enable unimem probe (auto-on for aarch64-apple-darwin in build.rs ideally; explicit for now)
probe-unimem = ["dep:unimem"]
