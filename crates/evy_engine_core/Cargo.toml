[package]
name = "evy_engine_core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "evy: assembly crate — single entry point that wires platform_caps, ecs_storage, engine_dispatch, radio, semcon, diagnostic"

[dependencies]
evy_platform_caps = { path = "../evy_platform_caps" }
evy_ecs_storage = { path = "../evy_ecs_storage" }
evy_engine_dispatch = { path = "../evy_engine_dispatch" }
evy_engine_tasks = { path = "../evy_engine_tasks" }
evy_radio = { path = "../evy_radio" }
evy_semcon = { path = "../evy_semcon" }
evy_diagnostic = { path = "../evy_diagnostic" }

[dev-dependencies]
bbg = { workspace = true }
